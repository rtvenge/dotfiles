#!/bin/bash

#############
# My Alias' #
#############

#my custom alias'
alias ll='ls -al'
alias gs='git status'
alias gcwp='git add -A && git commit -am "upgrading wordpress core/themes/plugins"'
alias ga='git add -A'
alias gc='git commit -am'
alias edithosts='atom /etc/hosts'
#alias sites='cd ~/Dropbox/Sites/'
alias pythonserver="p -m SimpleHTTPServer"
alias hb="~/Dropbox/Hoverboard"
alias d="~/Dropbox"
alias v='vagrant'
alias tweet='t update'
alias twitter='t timeline -n 10'

# List all instances of Virtual Box
alias lsbox="ps aux | grep '[V]BoxHeadless' | awk '{ print \$2 }' | xargs -I {} lsof -a -d cwd -p {}"

alias ip="ifconfig |grep inet"

# To easily share file through command line
# https://github.com/dutchcoders/transfer.sh/
transfer() {
  # write to output to tmpfile because of progress bar
  tmpfile=$( mktemp -t transferXXX )
  curl --progress-bar --upload-file $1 https://transfer.sh/$(basename $1) >> $tmpfile;
  cat $tmpfile;
  rm -f $tmpfile;
}

alias transfer=transfer
