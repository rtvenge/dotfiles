name = "Tactile"
description = "Lando site"
working_dir = "~/Sites/lando/tactileinc"
group = "Lando"

[[tabs]]
name = "CLI"
# Your live session splits this tab into tactileinc + tactilefx panes;
# this schema doesn't expose a per-pane cwd, so both panes here start
# in working_dir - cd into ../tactilefx manually if needed.

[[tabs]]
name = "AI"
command = "claude"
